{% extends 'base.html.twig' %}

{% form_theme formRegister 'bootstrap_4_layout.html.twig' %}

{% block title %} Page d'enregistrement {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h1>Créer un compte</h1>

    {{ form_start(formRegister) }}

    {{ form_row (formRegister.nomAbonne, {'attr' : {'Label' : "Nom", 'placeholder' : "Nom" }}) }}
    {{ form_row (formRegister.prenomAbonne, {'attr' : {'placeholder' : "Prenom" }}) }}
    {{ form_row (formRegister.login, {'attr' : {'placeholder' : "Login" }}) }}
    {{ form_row (formRegister.password, {'attr' : {'placeholder' : "Password" }}) }}

    {{ form_end(formRegister) }}

</div>

{% endblock %}

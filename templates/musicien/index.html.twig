{% extends 'base.html.twig' %}

{% block activeMusicien %} active {% endblock %}

{% block title %}Musicien index{% endblock %}

{% block body %}
    <h1>Musicien index</h1>
    <form method="post" action="{{ path('musicien_indexF') }}">

        <input required type="text" name="filtre" placeholder="Filtre par nom" >

        <input type="submit" value="Filtrer" />

    </form>
    <br>
    <table class="table">
        <thead>
            <tr>
                <th>CodeMusicien</th>
                <th>NomMusicien</th>
                <th>PrenomMusicien</th>
                <th>AnneeNaissance</th>
                <th>AnneeMort</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for musicien in musiciens %}
            <tr>
                <td>{{ musicien.codeMusicien }}</td>
                <td>{{ musicien.nomMusicien }}</td>
                <td>{{ musicien.prenomMusicien }}</td>
                <td>{{ musicien.anneeNaissance }}</td>
                <td>{{ musicien.anneeMort }}</td>
                <td><img src= "{{ path('musicien_image', {'codeMusicien' : musicien.codeMusicien}) }}"
                         alt="Pas d'image"/></td>
                <td>
                    <a href="{{ path('musicien_show', {'codeMusicien': musicien.codeMusicien}) }}">show</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#{% for nombre in nombres%}#}

    <div class="centre">
        <ul class="pagination pagination-sm">
            {% if numPage==1 %}
            <li class="page-item disabled">
             {% else %}
                <li class="page-item active">
              {% endif %}
                <a class="page-link" href="{{ path('musicien_index_pages',{'numeroPage' :  numPage-1}) }}">&laquo;</a>
            </li>
            <li class="page-item active">
                <a class="page-link" href="{{ path('musicien_index_pages',{'numeroPage' :  1}) }}"> 1</a>
            </li>
            <li class="page-item active">
                <a class="page-link" href="{{ path('musicien_index_pages',{'numeroPage' : numPage+1}) }}">&raquo;</a>
            </li>
        </ul>
    </div>

{#{% endfor %}#}

{% endblock %}
